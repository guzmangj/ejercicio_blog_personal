<nav class="navbar bg-dark">
  <div class="container">
    <a class="navbar-brand text-light bg-dark" href="/">Hack the Blog</a>
    <div>
      <% if( locals.isAuthenticated) { %>
      <a class="btn btn-primary" href="/panel" role="button">Admin</a>
      <a class="btn btn-danger" href="/logout" role="button">Logout</a>
      <p style="color: white ">Welcome, <%=locals.user.firstname%></p>
      <% }else { %>
      <a class="btn btn-secondary" href="/login" role="button">Login</a>
      <% } %>
      
    </div>
  </div>
</nav>
